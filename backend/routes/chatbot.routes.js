import express from \\\"express\\\";\nimport { processUserCommand } from \\\"../controllers/chatbot.controller.js\\\";\n// import { protectRoute } from \\\"../middleware/protectRoute.js\\\"; // You might want to protect this route\n\nconst router = express.Router();\n\n// POST /api/chatbot/command\n// Expects { command: string, currentPath: string, availableRoutes: string[] } in body\nrouter.post(\\\"/command\\\", processUserCommand); // Consider adding protectRoute if only logged-in users can use it\n\nexport default router;\n 